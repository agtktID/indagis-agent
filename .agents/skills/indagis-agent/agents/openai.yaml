interface:
  display_name: "Indagis Agent"
  short_description: "Repo-specific patterns and workflows for indagis-agent"
  default_prompt: "Use the indagis-agent repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true